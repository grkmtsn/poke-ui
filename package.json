{
  "name": "poke-ui",
  "private": true,
  "scripts": {
    "build:core": "cd ./core && yarn build",
    "docs": "yarn build:core && cd ./docs && yarn dev --port=3001"
  },
  "workspaces": [
    "core",
    "docs"
  ]
}
